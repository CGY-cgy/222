### 1. 数据看板接口 
#### 1.1 获取统计概览 
 
 - **用途**：获取系统统计概览数据，包括用户总数、活跃用户数、新增用户数和留存率。 
 - **请求方式**：GET  
 - **建议路径**：`/api/dashboard/stats`
| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 today） |
| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.totalUsers` | number | 用户总数 | 
| `data.activeUsers` | number | 活跃用户数 | 
| `data.newUsers` | number | 新增用户数 | 
| `data.retentionRate` | number | 留存率 | 

#### 1.2 获取最近活动 
 
 - **用途**：获取系统最近的用户活动记录。 
 - **请求方式**：GET  
 - **建议路径**：`/api/dashboard/activities`
| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `limit` | number | 返回数量（默认 10，最大 50） | 
| `offset` | number | 偏移量（默认 0） |

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码 | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.activities` | array | 活动列表 | 
| `data.activities[].id` | number | 活动ID | 
| `data.activities[].user` | string | 用户名 | 
| `data.activities[].action` | string | 操作类型 | 
| `data.activities[].time` | string | 操作时间 | 
| `data.activities[].ip` | string | 用户IP | 
| `data.total` | number | 活动总数 | 

#### 1.3 获取增长趋势数据 
 
 - **用途**：获取系统用户增长趋势数据。 
 - **请求方式**：GET  
 - **建议路径**：`/api/dashboard/growth-trend`
| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 week） |
| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码 | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 时间标签列表 | 
| `data.datasets` | array | 数据集列表 | 
| `data.datasets[].label` | string | 数据标签 | 
| `data.datasets[].data` | array | 数据值列表 |

### 2. 用户管理接口
#### 2.1 获取用户列表 
 
 - **用途**：获取系统用户列表，支持搜索、筛选和分页。 
 - **请求方式**：GET  
 - **建议路径**：`/api/users`
| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `search` | string | 搜索关键词（匹配姓名 / 手机号 / 邮箱） | 
| `role` | string | 角色筛选（枚举：admin/user/guest） | 
| `status` | string | 状态筛选（active/inactive） | 
| `page` | number | 页码（默认 1） | 
| `pageSize` | number | 每页数量（默认 20，最大 100） |

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.users` | array | 用户列表 | 
| `data.users[].id` | number | 用户ID | 
| `data.users[].name` | string | 用户名 | 
| `data.users[].phone` | string | 手机号 | 
| `data.users[].email` | string | 邮箱 | 
| `data.users[].role` | string | 用户角色 | 
| `data.users[].status` | string | 用户状态 | 
| `data.users[].createdAt` | string | 创建时间 | 
| `data.total` | number | 总用户数 | 
| `data.page` | number | 当前页码 | 
| `data.pageSize` | number | 每页数量 | 
| `data.totalPages` | number | 总页数 |
#### 2.2 获取用户详情 

- **用途**：获取指定用户的详细信息，包括基本信息和个人资料。 
- **请求方式**：GET  
- **建议路径**：`/api/users/:id` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `id` | number | 用户 ID | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.id` | number | 用户 ID | 
| `data.name` | string | 用户姓名 | 
| `data.phone` | string | 手机号 | 
| `data.email` | string | 邮箱 | 
| `data.role` | string | 用户角色 | 
| `data.status` | string | 用户状态 | 
| `data.createdAt` | string | 创建时间 | 
| `data.lastLogin` | string | 最后登录时间 | 
| `data.profile.avatar` | string | 用户头像 | 
| `data.profile.gender` | string | 用户性别 | 
| `data.profile.birthday` | string | 用户生日 |
#### 2.3 更新用户信息 

- **用途**：更新指定用户的基本信息和个人资料。 
- **请求方式**：PUT  
- **建议路径**：`/api/users/:id` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `id` | number | 用户 ID | 
| `body` | object | 见下表 | 

| Body 参数 | 类型 | 说明 | 
|---------|------|------| 
| `name` | string | 姓名 | 
| `email` | string | 邮箱（需校验格式） | 
| `role` | string | 角色（admin/user/guest） | 
| `profile` | object | 个人资料（头像 / 性别 / 生日） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.id` | number | 用户 ID | 
| `data.name` | string | 用户姓名 | 
| `data.phone` | string | 手机号 | 
| `data.email` | string | 邮箱 | 
| `data.role` | string | 用户角色 | 
| `data.status` | string | 用户状态 |
#### 2.4 更改用户状态 

- **用途**：更改指定用户的状态（激活/停用）。 
- **请求方式**：PATCH  
- **建议路径**：`/api/users/:id/status` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `id` | number | 用户 ID | 
| `status` | string | 新状态（active/inactive） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.id` | number | 用户 ID | 
| `data.status` | string | 更新后的用户状态 |
#### 2.5 导出用户数据 

- **用途**：导出系统用户数据，支持多种格式和筛选条件。 
- **请求方式**：GET  
- **建议路径**：`/api/users/export` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `format` | string | 导出格式（xlsx/csv/pdf，默认 xlsx） | 
| `search` | string | 搜索关键词 | 
| `role` | string | 角色筛选 | 
| `status` | string | 状态筛选 | 

**响应**：文件流（Content-Type: application/octet-stream）
### 3. 数据分析接口（优化核心：补充缺省值、统一枚举）
#### 3.1 获取统计数据 

- **用途**：获取系统访问统计数据，包括总访问量、平均访问时长、跳出率和转化率。 
- **请求方式**：GET  
- **建议路径**：`/api/analytics/stats` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.totalVisits` | number | 总访问量 | 
| `data.avgVisitDuration` | number | 平均访问时长（秒） | 
| `data.bounceRate` | number | 跳出率（百分比） | 
| `data.conversionRate` | number | 转化率（百分比） |
#### 3.2 获取流量趋势 

- **用途**：获取系统访问流量的趋势数据，支持按时间范围查看。 
- **请求方式**：GET  
- **建议路径**：`/api/analytics/traffic-trend` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 时间标签列表 | 
| `data.datasets` | array | 数据集列表 | 
| `data.datasets[].label` | string | 数据标签 | 
| `data.datasets[].data` | array | 数据值列表 |
#### 3.3 获取设备分布 

- **用途**：获取系统访问设备的分布情况，包括移动端、桌面端和平板设备的占比。 
- **请求方式**：GET  
- **建议路径**：`/api/analytics/device-distribution` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 设备类型标签列表 | 
| `data.data` | array | 各设备类型的占比数据 | 
| `data.total` | number | 总数，方便前端计算占比 |
#### 3.4 获取热图数据 

- **用途**：获取系统指定日期的热图数据，用于展示用户访问热点分布。 
- **请求方式**：GET  
- **建议路径**：`/api/analytics/heatmap` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `date` | string | 日期（格式 YYYY-MM-DD） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.data` | array | 热图数据点数组 | 
| `data.data[].x` | number | 数据点X坐标 | 
| `data.data[].y` | number | 数据点Y坐标 | 
| `data.data[].value` | number | 数据点值 |
#### 3.5 导出分析数据 

- **用途**：导出系统分析数据，支持多种格式和时间范围。 
- **请求方式**：GET  
- **建议路径**：`/api/analytics/export` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `format` | string | 导出格式（xlsx/csv/pdf，默认 xlsx） | 
| `range` | string | 时间范围（默认 month） | 

**响应**：文件流
### 4. 会员管理接口（优化核心：补充业务字段、统一响应）
#### 4.1 获取会员统计 

- **用途**：获取系统会员的统计数据，包括收入、会员数、转化率和新增会员数等。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/stats` 

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.revenue` | number | 会员收入 | 
| `data.members` | number | 会员总数 | 
| `data.conversion` | number | 会员转化率 | 
| `data.arpu` | number | 平均每用户收入 | 
| `data.newMembers` | number | 新增会员数 |
#### 4.2 获取转化漏斗 

- **用途**：获取会员转化漏斗数据，展示从注册用户到付费会员的转化路径。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/funnel`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.steps` | array | 转化漏斗步骤数组 | 
| `data.steps[].name` | string | 步骤名称 | 
| `data.steps[].value` | number | 步骤值 | 
| `data.steps[].rate` | number | 转化率 |
#### 4.3 获取套餐分布 

- **用途**：获取会员套餐分布数据，展示不同套餐类型的用户数量分布。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/package-distribution`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 套餐类型标签列表 | 
| `data.data` | array | 各套餐类型的用户数量 | 
| `data.total` | number | 总数 |
#### 4.4 获取收入趋势 

- **用途**：获取会员收入趋势数据，展示不同时间段的收入变化和环比增长率。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/revenue-trend`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 时间标签列表 | 
| `data.data` | array | 各时间段的收入数据 | 
| `data.growthRate` | array | 环比增长率列表 |
#### 4.5 获取留存率 

- **用途**：获取会员留存率数据，展示不同天数的用户留存情况和平均留存率。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/retention`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.day1` | number | 1天留存率 | 
| `data.day7` | number | 7天留存率 | 
| `data.day30` | number | 30天留存率 | 
| `data.day90` | number | 90天留存率 | 
| `data.avgRetention` | number | 平均留存率 |
#### 4.6 获取续费数据 

- **用途**：获取会员续费数据，展示不同续费次数的用户分布和整体续费率。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/renewal`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 month） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 续费次数标签列表 | 
| `data.data` | array | 各续费次数的用户数量 | 
| `data.renewalRate` | number | 整体续费率 |
#### 4.7 导出会员报告 

- **用途**：导出会员数据报告，支持多种格式和时间范围。 
- **请求方式**：GET  
- **建议路径**：`/api/membership/export`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `format` | string | 导出格式（xlsx/csv/pdf，默认 xlsx） | 
| `range` | string | 时间范围（默认 month） | 

**响应**：文件流（Content-Type: application/octet-stream）
### 5. 安全管理接口（优化核心：补充安全字段、增强可操作性）
#### 5.1 获取安全评分 

- **用途**：获取系统安全评分和状态信息。 
- **请求方式**：GET  
- **建议路径**：`/api/security/score`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.score` | number | 安全评分 | 
| `data.status` | string | 安全状态 | 
| `data.message` | string | 安全提示信息 | 
| `data.riskItems` | number | 风险项数量 |
#### 5.2 获取安全统计 

- **用途**：获取系统安全统计数据，包括请求数、可用性和告警信息。 
- **请求方式**：GET  
- **建议路径**：`/api/security/stats`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 week） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.requests` | number | 请求总数 | 
| `data.availability` | number | 系统可用性百分比 | 
| `data.blocked` | number | 被阻止的请求数 | 
| `data.alerts` | number | 告警总数 | 
| `data.highRiskAlerts` | number | 高危告警数 |
#### 5.3 获取安全事件趋势 

- **用途**：获取系统安全事件趋势数据，展示不同时间段的安全事件变化。 
- **请求方式**：GET  
- **建议路径**：`/api/security/events-trend`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 week） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 时间标签列表 | 
| `data.datasets` | array | 数据集列表 | 
| `data.datasets[].label` | string | 数据标签 | 
| `data.datasets[].data` | array | 安全事件数量数据 |
#### 5.4 获取安全事件分布 

- **用途**：获取系统安全事件类型分布数据，展示不同类型安全事件的数量。 
- **请求方式**：GET  
- **建议路径**：`/api/security/events-distribution`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `range` | string | 时间范围（默认 week） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.labels` | array | 安全事件类型标签列表 | 
| `data.data` | array | 各类型安全事件的数量 | 
| `data.total` | number | 安全事件总数 |
#### 5.5 获取安全日志 

- **用途**：获取系统安全日志列表，支持分页和筛选。 
- **请求方式**：GET  
- **建议路径**：`/api/security/logs`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `page` | number | 页码（默认 1） | 
| `pageSize` | number | 每页数量（默认 20，最大 100） | 
| `type` | string | 日志类型（security/system/operation） | 
| `status` | string | 状态（warning/error/info） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.logs` | array | 安全日志列表 | 
| `data.logs[].id` | number | 日志ID | 
| `data.logs[].time` | string | 日志时间 | 
| `data.logs[].type` | string | 日志类型 | 
| `data.logs[].desc` | string | 日志描述 | 
| `data.logs[].status` | string | 日志状态 | 
| `data.logs[].ip` | string | 用户IP | 
| `data.logs[].handleStatus` | string | 处理状态 | 
| `data.total` | number | 日志总数 | 
| `data.page` | number | 当前页码 | 
| `data.pageSize` | number | 每页数量 | 
| `data.totalPages` | number | 总页数 |
#### 5.6 获取安全漏洞 

- **用途**：获取系统安全漏洞列表，支持按严重程度筛选。 
- **请求方式**：GET  
- **建议路径**：`/api/security/vulnerabilities`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `severity` | string | 严重程度（high/medium/low） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.vulnerabilities` | array | 安全漏洞列表 | 
| `data.vulnerabilities[].id` | number | 漏洞ID | 
| `data.vulnerabilities[].title` | string | 漏洞标题 | 
| `data.vulnerabilities[].severity` | string | 严重程度 | 
| `data.vulnerabilities[].cvss` | number | CVSS评分 | 
| `data.vulnerabilities[].impact` | string | 影响范围 | 
| `data.vulnerabilities[].desc` | string | 漏洞描述 | 
| `data.vulnerabilities[].fixSuggestion` | string | 修复建议 | 
| `data.vulnerabilities[].fixStatus` | string | 修复状态 | 
| `data.total` | number | 漏洞总数 |
#### 5.7 运行安全扫描 

- **用途**：启动系统安全扫描任务。 
- **请求方式**：POST  
- **建议路径**：`/api/security/scan`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.scanId` | string | 扫描任务ID | 
| `data.estimatedTime` | string | 预计耗时 |
#### 5.8 获取扫描结果 

- **用途**：获取指定安全扫描任务的结果信息。 
- **请求方式**：GET  
- **建议路径**：`/api/security/scan/:scanId`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `scanId` | string | 扫描任务ID | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.scanId` | string | 扫描任务ID | 
| `data.status` | string | 扫描状态 | 
| `data.startTime` | string | 开始时间 | 
| `data.endTime` | string | 结束时间 | 
| `data.results` | object | 扫描结果统计 | 
| `data.results.high` | number | 高危漏洞数量 | 
| `data.results.medium` | number | 中危漏洞数量 | 
| `data.results.low` | number | 低危漏洞数量 | 
| `data.details` | array | 详细扫描结果 |
#### 5.9 生成安全报告 

- **用途**：生成并导出系统安全报告，支持多种格式和时间范围。 
- **请求方式**：GET  
- **建议路径**：`/api/security/report`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `format` | string | 报告格式（xlsx/csv/pdf，默认 pdf） | 
| `range` | string | 时间范围（默认 month） | 

**响应**：文件流（Content-Type: application/octet-stream）
### 6. 系统设置接口（优化核心：补充字段、规范操作）
#### 6.1 获取系统设置 

- **用途**：获取系统全局设置信息。 
- **请求方式**：GET  
- **建议路径**：`/api/settings`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.language` | string | 系统语言 | 
| `data.region` | string | 系统区域 | 
| `data.darkMode` | boolean | 是否开启深色模式 | 
| `data.compactMode` | boolean | 是否开启紧凑模式 | 
| `data.animations` | boolean | 是否开启动画效果 | 
| `data.notifications` | object | 通知设置 | 
| `data.notifications.system` | boolean | 系统通知开关 | 
| `data.notifications.security` | boolean | 安全通知开关 | 
| `data.notifications.feedback` | boolean | 反馈通知开关 | 
| `data.notifications.email` | boolean | 邮件通知开关 | 
| `data.updateAt` | string | 最后更新时间 |
#### 6.2 更新系统设置 

- **用途**：更新系统全局设置信息。 
- **请求方式**：PUT  
- **建议路径**：`/api/settings`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `body` | object | 设置对象 | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.language` | string | 系统语言 | 
| `data.darkMode` | boolean | 是否开启深色模式 | 
| `data.updateAt` | string | 最后更新时间 |
#### 6.3 获取 API 密钥列表

- **用途**：获取系统API密钥列表信息。
- **请求方式**：GET  
- **建议路径**：`/api/settings/api-keys`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.keys` | array | API密钥列表 | 
| `data.keys[].id` | number | 密钥ID | 
| `data.keys[].name` | string | 密钥名称 | 
| `data.keys[].key` | string | API密钥 | 
| `data.keys[].createdAt` | string | 创建时间 | 
| `data.keys[].status` | string | 密钥状态 | 
| `data.keys[].expireAt` | string | 过期时间 |
#### 6.4 创建 API 密钥

- **用途**：创建新的系统API密钥。
- **请求方式**：POST  
- **建议路径**：`/api/settings/api-keys`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `name` | string | 密钥名称 | 
| `type` | string | 密钥类型（live/test） | 
| `expire` | number | 有效期（天，默认 365） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.id` | number | 密钥ID | 
| `data.name` | string | 密钥名称 | 
| `data.key` | string | API密钥 | 
| `data.type` | string | 密钥类型 | 
| `data.createdAt` | string | 创建时间 | 
| `data.expireAt` | string | 过期时间 | 
| `data.status` | string | 密钥状态 |
#### 6.5 撤销 API 密钥

- **用途**：撤销指定的API密钥。
- **请求方式**：DELETE  
- **建议路径**：`/api/settings/api-keys/:id`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `id` | number | 密钥 ID | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data` | null | 无数据返回 |
#### 6.6 获取缓存信息

- **用途**：获取系统缓存信息。
- **请求方式**：GET  
- **建议路径**：`/api/settings/cache`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.cache` | array | 缓存信息列表 | 
| `data.cache[].name` | string | 缓存名称 | 
| `data.cache[].size` | string | 缓存大小 | 
| `data.cache[].desc` | string | 缓存描述 | 
| `data.cache[].lastClean` | string | 最后清理时间 |
#### 6.7 清理缓存

- **用途**：清理指定的系统缓存。
- **请求方式**：DELETE  
- **建议路径**：`/api/settings/cache/:name`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `name` | string | 缓存名称（或 'all' 清理全部） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.cleaned` | string | 清理的缓存名称 | 
| `data.freedSize` | string | 释放的空间大小 |
#### 6.8 获取功能开关

- **用途**：获取系统功能开关列表信息。
- **请求方式**：GET  
- **建议路径**：`/api/settings/features`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.features` | array | 功能开关列表 | 
| `data.features[].name` | string | 功能名称 | 
| `data.features[].enabled` | boolean | 是否启用 | 
| `data.features[].status` | string | 功能状态 | 
| `data.features[].description` | string | 功能描述 | 
| `data.features[].updateAt` | string | 更新时间 |
#### 6.9 更新功能开关

- **用途**：更新指定功能的开关状态。
- **请求方式**：PATCH  
- **建议路径**：`/api/settings/features/:name`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `name` | string | 功能名称 | 
| `enabled` | boolean | 是否启用 | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.name` | string | 功能名称 | 
| `data.enabled` | boolean | 是否启用 | 
| `data.updateAt` | string | 更新时间 |
#### 6.10 导出系统设置

- **用途**：导出系统设置信息。
- **请求方式**：GET  
- **建议路径**：`/api/settings/export`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `format` | string | 导出格式（xlsx/csv/pdf，默认 xlsx） | 

**响应**：文件流
#### 6.11 导入系统设置

- **用途**：导入系统设置信息。
- **请求方式**：POST  
- **建议路径**：`/api/settings/import`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `file` | 文件 | 设置文件 | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.imported` | array | 导入的配置项列表 | 
| `data.updateAt` | string | 更新时间 |
### 7. 公共接口（优化核心：补充错误处理、规范响应）
#### 7.1 上传文件

- **用途**：上传文件到系统服务器。
- **请求方式**：POST  
- **建议路径**：`/api/upload`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 
| `file` | 文件 | 要上传的文件（支持 jpg/png/pdf/xlsx/csv） | 
| `type` | string | 文件类型（avatar/document/other，默认 other） | 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.url` | string | 文件访问URL | 
| `data.fileName` | string | 文件名 | 
| `data.fileSize` | string | 文件大小 | 
| `data.uploadTime` | string | 上传时间 |
#### 7.2 获取系统状态

- **用途**：获取系统运行状态信息。
- **请求方式**：GET  
- **建议路径**：`/api/status`

| 输入参数 | 类型 | 说明 | 
|---------|------|------| 

| 输出参数 | 类型 | 说明 | 
|---------|------|------| 
| `code` | number | 状态码（如 200 表示成功） | 
| `success` | boolean | 请求是否成功 | 
| `message` | string | 描述信息 | 
| `data.status` | string | 系统状态 | 
| `data.version` | string | 系统版本 | 
| `data.uptime` | number | 运行时间（秒） | 
| `data.timestamp` | string | 当前时间戳 | 
| `data.cpuUsage` | string | CPU使用率 | 
| `data.memoryUsage` | string | 内存使用率 | 
| `data.diskUsage` | string | 磁盘使用率 |