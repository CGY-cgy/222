name: lingjing
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.11.0-56.0.dev <4.0.0' # 修改这里

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 确保 intl 是刚才报错要求的版本
  intl: ^0.20.2

  provider: ^6.1.1
  go_router: ^13.2.0
  dio: ^5.4.1
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  fl_chart: ^0.66.0
  permission_handler: ^11.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/